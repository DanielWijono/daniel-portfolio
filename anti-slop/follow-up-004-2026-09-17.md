# Antislop follow-up 004

Date: 2026-09-17
Approved findings: 1-10 in `anti-slop/audit-004-2026-09-17.md`
Result: **PASS for the approved scope**

## Changes

1. **Finding 1 resolved: every heading is now distinguishable out of context.** `R-32`, `C-4`
   - Moved the employer into the role heading as a nested `span.job-org`, so headings read "Senior iOS Developer Bank Negara Indonesia" instead of five near-identical role titles.
   - `.job-org` resets font family and size inside the heading, so the rendered appearance is unchanged: serif role, bronze sans employer beneath it.
   - Browser check: 16 headings, zero duplicates.

2. **Finding 2 resolved: every section now carries headings below `h2`.** `R-32`, `C-4`
   - Promoted the Skills group labels and both Education entries to `h3`.
   - `#skills` and `#education` previously contained zero `h3` elements; both now contribute to the document outline.

3. **Finding 3 resolved: roles without evidence no longer pose as full entries.** `C-3`, `C-5`
   - Replaced the three bullet-less entries (Asian Technology Solutions, Jack's Labs, PT Sola Interactive / OTOMOTO) with one compressed "Earlier iOS and Android roles" entry spanning 2017-2020, described as "Contract, agency, and product teams."
   - No contribution bullets were invented. Location details that only one of the three originally carried were dropped rather than generalized.
   - Browser check: zero `.job` blocks now contain neither bullets nor a descriptor.

4. **Finding 4 resolved: the Accenture descriptor no longer repeats employer marketing copy.** `R-16`
   - "Global consulting firm helping businesses build their digital core" became "Professional services and technology consulting firm."

5. **Finding 5 resolved: unverifiable soft-skill claims removed.** `C-5`, `C-3`
   - Deleted the soft-skills column entirely. The traits it claimed (teamwork, communication, time management) are already demonstrated with evidence in the Experience bullets, for example leading four developers and coordinating sprint delivery.

6. **Finding 6 resolved: the footer statement does closing work.** `R-31`, `C-3`
   - Replaced "At Quadrant, maintained a 97% crash-free rate." with the colophon "Silverius Daniel, Senior iOS Developer."
   - This introduces no new claim, stops duplicating the Experience bullet at `public/index.html:207`, and gives the closing slot the job of signing the page the way the masthead opens it. The contact row directly beneath it remains the next step.

7. **Finding 7 resolved: the descriptor pattern now has a rule.** `R-31`, `C-1`
   - Every Experience entry carries exactly one neutral factual descriptor, including Bank Negara Indonesia, which previously had none: "State-owned national bank in Indonesia." That restates what the Work section already says about the same employer rather than adding a claim.

8. **Finding 8 resolved: Skills aligns with the hero and drops routine activities.** `C-1`, `C-3`
   - Added VIPER, which the hero already claimed but Skills omitted.
   - Removed "SDLC", "Code Review", "Pair Programming", and "Debugging" as ordinary practice rather than differentiating capability.
   - Regrouped the remainder into three named `h3` groups: Languages and frameworks, Architecture, Tooling and delivery.

9. **Finding 9 resolved: dead CSS deleted.** `C-1`
   - Removed the unused `--color-accent-2`, `--space-3xl`, and `--text-display` tokens.
   - Collapsed the three `.work-link` rules that branched on a `data-pending` state no element carried into a single rule with identical rendering. Browser check confirms zero `[data-pending]` elements and `grep` confirms zero remaining references.

10. **Finding 10 resolved: the page ships a meta description.** `C-1`
    - Added a 173-character description drawn from existing hero copy, introducing no new claims.

## Design record

Updated `DESIGN.md` with one-line reasons for the employer-in-heading change, the one-descriptor-per-employer rule, the compressed earlier-roles entry, the verifiable-only grouped Skills section, and the footer colophon.

## Verification

- Heading audit: 16 headings across `h1`/`h2`/`h3`, zero duplicate heading strings, every section contributing sub-headings.
- Contrast measured on rendered colors by painting each computed `oklch()` value to a canvas and reading back the pixel. Ten text pairs checked, zero failures; the lowest ratio was 6.10:1 against a 4.5:1 requirement.
- 320px: document width 320px, no horizontal overflow, zero off-screen controls, zero targets below 44 by 44px, zero elements with internal overflow. Page height dropped from 5,965px to 5,529px.
- 751px: no overflow, no undersized targets, masthead name on one line at 166 by 35px, confirming the audit-003 reflow threshold still holds.
- Desktop: no overflow; the Skills grid resolves to three equal columns and falls back to two, then one, as width decreases.
- Screenshots at desktop width confirm the Experience entry renders exactly as before the heading change, and that the Skills section reads as three scannable groups.
- Link count remains 13, matching audit-003; no interactive element was added or removed, so the verified tab order and destinations from that audit still stand.
- `grep` confirms zero em dashes, zero forbidden buzzwords, and zero references to the removed tokens or the removed inflated copy.
- Console: no errors or warnings on a fresh load.

## Still open, needs owner input

Carried forward unchanged from audit-004: Selected Work shows two items, one of which is a UI component rather than a shipped product, and the 2020-2022 Accenture period has no linkable artifact. This cannot be fixed without real evidence from the owner, and inventing it would breach `R-38`.

## Delivery gate

- **Hard Gate: PASS.** `R-32` now passes; heading semantics and landmarks are complete. All other Hard Gate rules were already passing and were re-checked.
- **Purpose-Gate: PASS.** Every new decision has a written reason in `DESIGN.md`.
- **Liveliness: PASS.** ENERGY 2 / RHYTHM 2 / MOTION 1 intact; the visual identity is unchanged by this pass.
- **Craftsmanship and Quality Locks: PASS.** All ten approved findings are closed.
